import {/*LOAD_ITEMS, LOAD_ITEMS_PENDING, LOAD_ITEMS_ABORTED*/} from '../../actions';

//-------------------------------------------------------------------
// APPLICATION STORE
//-------------------------------------------------------------------
export const application = (state = { isLoading: false }, {type}) => {
    /* eslint-disable indent */
    switch (type) {

        default:
            return state;
    }
    /* eslint-enable indent */
};
