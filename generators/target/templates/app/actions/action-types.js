import LoginActionTypes from '../containers/login/actions/action-types';

// Expose cross container Action Types on root.
const RootActionTypes = {...LoginActionTypes};
export default RootActionTypes;
